{
  "name": "@stencil/forms",
  "version": "0.0.1",
  "description": "Stencil Forms",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "node scripts/clean.js",
    "start": "cd src/test && stencil build --dev --watch --serve"
  },
  "license": "MIT",
  "devDependencies": {
    "@stencil/core": "^1.13.0",
    "typescript": "^3.9.2"
  },
  "dependencies": {
    "@stencil/store": "^1.3.0"
  }
}
